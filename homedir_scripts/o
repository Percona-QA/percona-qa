COUNT=$(ls -t $1/*_out | head -n1 | wc -l)
if [ $COUNT -gt 25 ]; then
  echo "The file $(ls -t $1/*_out | head -n1) is >25 lines long, not displaying it here"
else
  cat $(ls -t $1/*_out | head -n1)
fi
