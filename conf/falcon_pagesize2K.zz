$tables = {
        rows => [0, 1, 10, 100],
	pk => [ undef, 'int auto_increment' ],
	engines => ['Falcon']
};

$fields = {
        types => [ 'bit', 'int', 'char(130)', 'enum', 'set', 'blob' ],
        indexes => [ undef, 'key' ],
        charsets => ['utf8', 'latin1' ]
};

$data => {
        numbers => [ 'digit', 'null', undef ],
        strings => [ 'letter', 'english', 'null' ],
        blobs => [ 'english', 'data' ]
}
