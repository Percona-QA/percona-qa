# pg_tde Pytest Coverage Report — 2026-05-12

**Date:** 2026-05-12
**Previous baseline:** `baseline_2026-05-11.md`
**Scope:** Delta + current state of pytest coverage for pg_tde

---

## Executive summary

| Metric | Baseline (2026-05-11) | Now (2026-05-12) | Δ |
|---|---|---|---|
| Pytest test files | 11 | **12** | +1 (new `test_waldump.py`) |
| Pytest test methods | ~221 | **~267** | **+46** |
| Quality split (deep / shallow / weak) | 57% / 39% / 1% | **67% / 30% / 3%** | +10pp deep |
| Open 🔴 high-priority gaps | 7 | **3** | −4 (closed: cipher, segsize, enforce, verify/delete, basebackup-E, waldump, logical+wal_enc, crash+wal_enc) |

### Top headline

**Eight zero-coverage gaps from the baseline are now closed.** All of the
high-priority "policy and diagnostic" items the previous report flagged
have at least one dedicated test; some are deeply covered (10 tests in
`TestTdeEnforceEncryption` alone).

Quality split improved meaningfully — new tests are uniformly **deep**
(multiple assertions, state + behaviour, encrypted/plaintext on-disk
byte checks, restart persistence, error path verification).

---

## 1. What changed since baseline

### New test classes / methods (9 additions, ~46 tests)

| New module / class | Tests | Purpose |
|---|---|---|
| `test_encryption.py::TestTdeCipher` | 6 | `pg_tde.cipher` GUC (aes_128 / aes_256) with byte-level ciphertext divergence proof |
| `test_encryption.py::TestWalSegmentSizeWithEncryption` | 2 (parametrized) | WAL encryption across 1 MB and 64 MB segment sizes |
| `test_encryption.py::TestTdeEnforceEncryption` | 10 | `pg_tde.enforce_encryption` GUC: blocks heap CREATE / CTAS / ALTER; allows tde_heap; existing heap tables stay accessible; persists across restart |
| `test_encryption.py::TestTdeVerifyDeleteKeyApis` | 6 | `pg_tde_verify_*` and `pg_tde_delete_*` SQL APIs (with skip-on-absent helpers) |
| `test_pg_basebackup.py::TestPgTdeBaseBackupWalEncryption` | 2 | `pg_tde_basebackup -E` (encrypted target) — both positive and warning-contract |
| `test_waldump.py` (new file) | 2 | `pg_waldump` on encrypted WAL — decode validation + pg_tde rmgr registration |
| `test_replication.py::TestLogicalReplication::test_logical_replication_with_wal_encryption` | 1 | Logical replication with `wal_encrypt = on` on both sides; LSN-polled (no sleeps); subscriber pg_wal/ ciphertext check |
| `test_recovery.py::TestCrashRecovery::test_crash_recovery_with_wal_encryption` | 1 | SIGKILL + restart + post-checkpoint encrypted-WAL replay path |
| `test_tde_rewind_advanced.py::TestTdeRewindExtended` (PG-2330/2357) | 2 | Restart-old-primary-before-rewind regression; single + 5×-stress |
| `test_pgbackrest.py::TestPgBackRestMatrix` (10 scenarios) | 10 | full, delta, standby, PITR-time, PITR-LSN, PITR-XID, selective-db, force, chain, check |
| `test_pgbackrest.py::TestPgBackRestAdvancedAndNegative` | 4 | Key rotation in backup chain, missing library, missing WAL, concurrent DDL |

### Library / infrastructure changes

| Change | Where | Why |
|---|---|---|
| `tde_basebackup(encrypt_wal=…)` kwarg | `lib/tde.py` | Discoverable `-E` toggle with auto-detect from `pg_tde.wal_encrypt` |
| `create_standby_from_backup(encrypt_wal=…)` kwarg | `lib/replication.py` | Threads the same option through replication setup |
| `BackupManager.restore()` extended | `lib/backup.py` | Adds restore_type, target, target_action, db_include, force kwargs |
| `BackupManager.wait_for_wal_archive()` | `lib/backup.py` | Polls `pg_stat_archiver` instead of sleeping; CHECKPOINT-then-switch trick |
| `_run()` error reporting | `lib/backup.py` | RuntimeError with stderr/stdout — surfaces pgBackRest failures actionably |
| `spool-path` per-test | `lib/backup.py` | Avoids the [053] permission denied on default `/var/spool/pgbackrest` |
| `waldump` pytest marker | `pyproject.toml` | New mark for the diagnostic-tool tests |

---

## 2. Coverage matrix — current state

Legend: ✅ deep · 🟡 shallow · ❌ missing · ⚠ partial · 🆕 new since baseline

### 2.1 GUCs

| GUC | Pytest | Quality | Δ |
|---|---|---|---|
| `pg_tde.wal_encrypt` | ✅ `TestWalEncryption` + WAL-enc cross-cuts | ✅ deep | unchanged |
| `pg_tde.cipher` | 🆕 `TestTdeCipher` (6 tests) | ✅ deep | ❌ → ✅ |
| `pg_tde.enforce_encryption` | 🆕 `TestTdeEnforceEncryption` (10 tests) | ✅ deep | ❌ → ✅ |
| `pg_tde.inherit_global_providers` | ❌ | ❌ missing | unchanged — low priority, behaviour-default |

### 2.2 Key providers

| Provider | Pytest | Quality | Δ |
|---|---|---|---|
| File | ✅ `TestKeyManagement`, tde_primary fixture | ✅ deep | unchanged |
| HashiCorp Vault v2 | ⚠ `@pytest.mark.vault` (skipped w/o server) | 🟡 shallow | unchanged |
| **Vault v2 namespace (2.1)** | ❌ | ❌ missing | unchanged — 🔴 still open |
| **KMIP (generic)** | ❌ | ❌ missing | unchanged — 🔴 still open |
| OpenBao | ❌ | ❌ missing | unchanged — bash exists |
| Provider migration | 🟡 `test_multiple_key_providers` | 🟡 shallow | unchanged |
| **`pg_tde_change_key_provider` CLI** | ❌ | ❌ missing | unchanged — 🔴 still open |

### 2.3 SQL functions

| Function group | Pytest | Quality | Δ |
|---|---|---|---|
| `pg_tde_add_global_key_provider_*` | ✅ | ✅ deep | unchanged |
| `pg_tde_add_database_key_provider_*` | ⚠ implicit | 🟡 shallow | unchanged |
| `pg_tde_change_*_key_provider_*` | ⚠ partial | 🟡 shallow | unchanged |
| `pg_tde_delete_*_key_provider` | ❌ | ❌ missing | unchanged |
| `pg_tde_create_key_using_*` | ✅ | ✅ deep | unchanged |
| `pg_tde_set_key_using_*` | ✅ | ✅ deep | unchanged |
| `pg_tde_set_server_key_using_*` | ✅ | ✅ deep | unchanged |
| `pg_tde_set_default_key_using_*` | 🆕 `TestTdeVerifyDeleteKeyApis` exercises it | ✅ deep | 🟡 → ✅ |
| **`pg_tde_verify_*`** | 🆕 `TestTdeVerifyDeleteKeyApis` (3 verify variants + post-rotation) | ✅ deep | ❌ → ✅ |
| **`pg_tde_delete_*`** | 🆕 `TestTdeVerifyDeleteKeyApis` (2 delete variants with re-create) | ✅ deep | ❌ → ✅ |
| `pg_tde_key_info()` etc. | ✅ | ✅ deep | unchanged |
| `pg_tde_is_encrypted(REGCLASS)` | ✅ | ✅ deep | unchanged |

### 2.4 CLI tools

| Tool | Pytest | Quality | Δ |
|---|---|---|---|
| `pg_tde_basebackup` | ✅ `test_basebackup_with_tde` + HA failover | 🟡 shallow on plain backup | unchanged |
| **`pg_tde_basebackup -E`** | 🆕 `TestPgTdeBaseBackupWalEncryption` (2 tests) | ✅ deep | ❌ → ✅ |
| **`pg_tde_change_key_provider`** | ❌ | ❌ missing | unchanged — 🔴 still open |
| `pg_tde_archive_decrypt` / `pg_tde_restore_encrypt` | ⚠ via PITR test | 🟡 shallow | unchanged |
| **`pg_tde_waldump` (Percona `pg_waldump`)** | 🆕 `test_waldump.py` (2 tests) | ✅ deep | ❌ → ✅ |
| `pg_tde_checksums` | ✅ `TestChecksums` (2 tests) | 🟡 shallow | unchanged |
| `pg_tde_resetwal` | 🟡 `test_pg_resetwal` (return code only) | 🟡 shallow | unchanged |
| `pg_tde_rewind` | ✅ `test_tde_rewind_advanced.py` (66 tests) | ✅ deep | unchanged |

### 2.5 Major feature areas

| Area | Pytest | Quality | Δ |
|---|---|---|---|
| Basic tde_heap encryption | ✅ `TestTdeSetup` | ✅ deep | unchanged |
| Dynamic encryption state | ✅ `TestDynamicEncryptionState` | ✅ deep | unchanged |
| Template database creation | ⚠ partial | 🟡 shallow | unchanged |
| ALTER DATABASE SET TABLESPACE | ✅ 5 tests | ✅ deep | unchanged |
| Partitioned tables | ❌ standalone | ❌ missing | unchanged |
| Streaming replication | ✅ | ✅ deep | unchanged |
| **Logical replication + WAL enc** | 🆕 `test_logical_replication_with_wal_encryption` | ✅ deep | 🟡 → ✅ |
| Standby promotion | 🟡 (1 test) | 🟡 shallow | unchanged |
| Crash recovery (TDE) | ✅ `test_data_survives_crash_tde` | ✅ deep | unchanged |
| **Crash recovery + WAL enc** | 🆕 `test_crash_recovery_with_wal_encryption` | ✅ deep | 🟡 → ✅ |
| pgBackRest matrix | 🆕 `TestPgBackRestMatrix` (10 scenarios) + advanced (4) | ✅ deep | 🟡 → ✅ |
| pg_basebackup | 🟡 (file-existence only) | 🟡 shallow | unchanged |
| Major upgrade (PPG → PSP / PSP → PSP) | ✅ 9 tests | ✅ deep | unchanged |
| Upgrade access methods | ✅ 5 tests | ✅ deep | unchanged |
| Upgrade WAL encryption paths | ✅ 4 tests | ✅ deep | unchanged |
| Minor upgrade (2.1.x → 2.1.x+1) | ✅ 31 tests | ✅ deep | unchanged |
| **WAL segsize 1 MB / 64 MB** | 🆕 `TestWalSegmentSizeWithEncryption` | ✅ deep | ❌ → ✅ |
| Bug reproductions | ✅ `test_bug_reproduction.py` + new PG-2330/2357 | ✅ deep | + |

---

## 3. Remaining gaps — prioritized

Three 🔴 high-priority items from the baseline are still open:

| # | Item | Bash equivalent | Effort |
|---|---|---|---|
| 1 | **KMIP key provider** (any vendor) | ❌ (only OpenBao bash) | large — needs local pykmip / OpenBao + KMIP test scaffolding |
| 2 | **Vault v2 namespace parameter** (pg_tde 2.1 new arg) | ❌ | medium — needs OpenBao container with namespaces |
| 3 | **`pg_tde_change_key_provider` CLI tool** | ✅ `pg_tde_change_key_provider_utility.sh` | medium — port the bash scenarios |

Medium-priority (🟡) gaps still open:

- Database-scoped key providers (vs global) — minimal pytest coverage
- `pg_tde_delete_*_key_provider` SQL functions — entirely untested
- OpenBao end-to-end (bash exists, no pytest)
- `pg_tde.inherit_global_providers` GUC
- Partitioned tde_heap standalone (only covered via upgrade tests)
- pgBackRest HA failover rebuild (bash has it; pytest only has basebackup HA path)
- `pg_tde_waldump --rmgr=list` on systems where it surfaces custom rmgrs (currently we skip this branch)

---

## 4. Remaining quality issues

| Concern | Files affected | Severity |
|---|---|---|
| Sleep-based polling | `test_pitr.py`, `test_tde_minor_upgrade.py` (2-3 s sleeps for WAL archiving) | medium — most tests already use deadline loops |
| Hardcoded `/tmp/pg_tde_*.per` keyfile paths | ~15 sites | low — blocks `pytest -n auto` parallel runs but works serially |
| Shallow subprocess checks | `test_recovery.py::TestWalUtilities`, `test_upgrade.py::TestUpgradeNegative*` (return-code-only) | low — fine for negative tests, weaker for utilities |
| Tautological assertion | `test_recovery.py::test_immediate_shutdown_recovery` (`int(count) >= 0`) | low — single instance |
| `test_basebackup_plain_cluster` / `test_basebackup_with_tde` | Only check `PG_VERSION` file exists | medium — could verify post-restore queries |

None of these are regressions; they're carry-over from the baseline. The
new tests added since 2026-05-11 do **not** introduce any of these
patterns.

---

## 5. Headlines per test file

| File | Tests | Quality strength |
|---|---|---|
| `test_tde_rewind_advanced.py` | 66+ | strongest — comprehensive rewind matrix |
| `test_encryption.py` | 48 | now broadest GUC + API coverage |
| `test_upgrade.py` | 47 | strongest plain-PG upgrade matrix |
| `test_tde_minor_upgrade.py` | 31 | best-structured (4-phase lifecycle) |
| `test_pgbackrest.py` | 17 | now near-complete (matrix + advanced + negative) |
| `test_replication.py` | 14 | streaming deep; logical+WAL enc new |
| `test_recovery.py` | 10 | now includes encrypted-WAL replay path |
| `test_bug_reproduction.py` | 7 | regression-only (PG-1805/1806/2330/2357) |
| `test_pg_basebackup.py` | 6 | now includes -E coverage |
| `test_pitr.py` | 2 | smallest; sleep-based |
| `test_waldump.py` | 2 | new — first coverage of the diagnostic tool |

---

## 6. Prioritized backlog (next items)

Top three to attack next, in order:

1. **`pg_tde_change_key_provider` CLI** — port the bash automation. Medium effort. Covers a real ops workflow (offline-server provider migration).
2. **`pg_tde_delete_*_key_provider` SQL** — small extension to `TestTdeVerifyDeleteKeyApis`. Pairs with the delete-key work just landed.
3. **KMIP or Vault-namespace coverage** — pick one. KMIP via pykmip is more universally applicable (pykmip is already in our setup deps). Vault-namespace is a smaller, more focused gap tied to a specific new feature.

Lower priority:
- Replace `time.sleep` with polling in the two remaining users (`test_pitr.py`, `test_tde_minor_upgrade.py` WAL-archiving paths)
- Deepen `test_basebackup_plain_cluster` / `test_basebackup_with_tde` to actually query restored data
- Add a standalone `test_partitioning.py` for partitioned tde_heap tables

---

## 7. Recommendations for the team

1. **Adopt the new patterns.** New tests added since the baseline use:
   - Polling-with-deadline instead of `time.sleep`
   - On-disk byte checks (encrypted-content / plaintext-leak markers)
   - Server-log scans for error phrases (decryption errors etc.)
   - Skip-on-missing-function probes for forward compatibility
   These should be the template for future tests.

2. **Adopt `--strict-markers` permanently.** All new markers (`waldump`)
   are registered in `pyproject.toml`. A typo in a marker now fails at
   collection, not at runtime.

3. **Keep `coverage_reports/` versioned.** Each report is dated so
   week-over-week deltas are visible. The skill `/tde-coverage report`
   regenerates this file with a fresh date.

---

*Generated from a fresh agent-driven audit of
`/Users/shahid/Percona/percona-qa/postgresql/pytest/tests/`. Re-run
weekly via the `/tde-coverage report` skill.*
